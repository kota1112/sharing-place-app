# config/database.yml
# PostgreSQL（Postgres.app / Homebrew / EnterpriseDB いずれでもOK）

default: &default
  adapter: postgresql
  encoding: unicode

  # コネクション数（指定がなければ 10）
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 10 } %>

  # 優先度: PGUSER → USER → "postgres"
  username: <%= ENV.fetch("PGUSER") { ENV.fetch("USER") { "postgres" } } %>

  # .env が無くても動くようにデフォルト値を入れておく
  host: <%= ENV.fetch("PGHOST", "127.0.0.1") %>
  port: <%= ENV.fetch("PGPORT", 5432) %>

  # パスワードが必要な環境（EDB など）は PGPASSWORD を設定して使う
  password: <%= ENV["PGPASSWORD"] %>

# ─────────────────────
# development（単一DB）
# ─────────────────────
development:
  <<: *default
  database: sharing_place_app_development

# ─────────────────────
# test（multi-DB）
# schema:load / migrate が test の全DB定義を見るため、
# cache/queue/cable も明示して nil を避ける
# ─────────────────────
test:
  primary:
    <<: *default
    database: <%= ENV.fetch("PGDATABASE_TEST") { "sharing_place_app_test" } %>
  cache:
    <<: *default
    database: <%= ENV.fetch("PGCACHE_DATABASE_TEST") { "sharing_place_app_test_cache" } %>
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    database: <%= ENV.fetch("PGQUEUE_DATABASE_TEST") { "sharing_place_app_test_queue" } %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    database: <%= ENV.fetch("PGCABLE_DATABASE_TEST") { "sharing_place_app_test_cable" } %>
    migrations_paths: db/cable_migrate

# ─────────────────────
# production（Render 等）
# 全ロールで同一の DATABASE_URL を使用する構成
# ※ DATABASE_URL には必ず sslmode=require を付与：
#   例）.../sharing_place_prod?sslmode=require
#   既にクエリがある場合）.../sharing_place_prod?foo=bar&sslmode=require
# ─────────────────────
production:
  primary:
    adapter: postgresql
    url: <%= ENV.fetch("DATABASE_URL") %>
    pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

  cache:
    adapter: postgresql
    url: <%= ENV.fetch("DATABASE_URL") %>
    migrations_paths: db/cache_migrate

  queue:
    adapter: postgresql
    url: <%= ENV.fetch("DATABASE_URL") %>
    migrations_paths: db/queue_migrate

  cable:
    adapter: postgresql
    url: <%= ENV.fetch("DATABASE_URL") %>
    migrations_paths: db/cable_migrate
